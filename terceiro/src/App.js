function App() {
  let styles = {
    backgroundColor: "#300",
    color: "#fff",
    display: "flex",
    flex: 1,
    height: "100vh",
  };

  return (
    <div style={styles}>
      <p>Veja o background</p>
    </div>
  );
}

export default App;
